/**
 * `@paperos/spec` — page and app spec schemas, validator and codegen (spec-builder / Quill, PAP-114..PAP-126).
 *
 * Wired but empty: this scaffold creates the package, its tsconfig and its test
 * so later issues only add files.
 */
export const SPEC_PACKAGE_ID = '@paperos/spec' as const;
