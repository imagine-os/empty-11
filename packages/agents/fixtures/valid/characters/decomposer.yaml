# yaml-language-server: $schema=../../../schema/character.schema.json
# Generated from docs/characters/atlas.md (linear-builder, 2026-09-19) for PAP-103; PAP-284 owns the live roster.
schemaVersion: 1
name: decomposer
displayName: Decomposer
role: Issue decomposer
kind: sub
reportsTo: atlas
parent: atlas
description: Use to turn briefs, epics and freeform notes into spec-complete issues with the eleven
  contract sections, sizes and blocks relations (PAP-93, PAP-95, PAP-103).
model: claude-fable-5-1
effort: xhigh
tools:
  allow:
    - Read
    - Glob
    - Grep
    - mcp__linear
mcpServers:
  - linear
access:
  - linear:admin
skills:
  - decompose-brief
  - linear-update
memory:
  path: docs/memory/characters/decomposer.md
  maxTokens: 1500
budget:
  perSessionUsd: 15
  maxTurns: 80
escalation:
  - when: anything outside the Decomposer lane described above
    action: escalate
    to: atlas
