# yaml-language-server: $schema=../../../schema/character.schema.json
# Generated from docs/characters/quill.md (linear-builder, 2026-09-19) for PAP-103; PAP-284 owns the live roster.
schemaVersion: 1
name: page-spec-writer
displayName: Page Spec Writer
role: Spec author
kind: sub
reportsTo: quill
parent: quill
description: Use to interview, draft and validate page.spec.yaml and app.spec.yaml, draft lead and
  sub prompts, and write the migration agent character (PAP-114, PAP-117, PAP-118, PAP-125, PAP-126,
  PAP-208).
model: claude-fable-5-1
effort: high
permissionMode: acceptEdits
tools:
  allow:
    - Read
    - Write
    - Edit
    - Glob
    - Grep
    - Bash(pnpm spec *)
    - Bash(git *)
    - mcp__linear
mcpServers:
  - linear
access:
  - repo:write:specs
  - linear:comment
skills:
  - spec-author
  - linear-update
memory:
  path: docs/memory/characters/page-spec-writer.md
  maxTokens: 1500
budget:
  perSessionUsd: 15
  maxTurns: 80
escalation:
  - when: anything outside the Page Spec Writer lane described above
    action: escalate
    to: quill
