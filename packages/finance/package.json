{
  "name": "@paperos/finance",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Finance module port types: the PayrollProvider interface (PAP-176/184) and friends. Pure TypeScript, no React, no database, no provider SDK.",
  "exports": {
    ".": "./src/index.ts",
    "./payroll": "./src/payroll/provider.ts"
  },
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "files": [
    "src"
  ],
  "scripts": {
    "lint": "biome check .",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run --passWithNoTests",
    "clean": "node -e \"require('node:fs').rmSync('coverage',{recursive:true,force:true})\""
  },
  "devDependencies": {
    "@biomejs/biome": "catalog:",
    "@paperos/config-ts": "workspace:*",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
