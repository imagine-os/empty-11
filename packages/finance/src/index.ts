/**
 * `@paperos/finance` barrel — finance module port types (no runtime, no provider SDK).
 *
 * Today this re-exports the `PayrollProvider` port decided by ADR 0007 (PAP-176).
 */
export * from './payroll/provider.js';
