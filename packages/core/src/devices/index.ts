/**
 * `@paperos/core/devices` — device and breakpoint matrix (PAP-14, ADR 0022).
 *
 * Sub-folder index so the curated `src/index.ts` barrel re-exports folders, not
 * files (rule R12 of the boundary lint, ADR 0026).
 */

export * from './matrix.js';
