/**
 * `@paperos/ui` — the PaperOS component library (design-system module).
 *
 * Empty on purpose: PAP-66 / PAP-67 / PAP-74 land tokens and components here.
 * The single string below is what the template's placeholder page renders, so
 * `pnpm dev` + editing this line demonstrates hot reload across the workspace.
 */
export const TEMPLATE_TITLE = 'PaperOS template';

/** Registry id of this package, used by the module manifest once it lands. */
export const UI_PACKAGE_ID = '@paperos/ui' as const;
