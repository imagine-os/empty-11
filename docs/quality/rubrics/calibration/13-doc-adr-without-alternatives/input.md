# Case 13: ADR missing sections

PR adds `docs/adr/0019-input-events.md`:

```markdown
# 0019 Input events

## Status
Accepted

## Context
We need one event shape for keyboard, pointer, touch, pen, remote and voice.

## Decision
A single InputEvent envelope with a source field...
```

No Consequences, no Alternatives rejected. The template (PAP-209) requires both.
