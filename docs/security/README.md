# docs/security

Threat model (ADR 0024 / PAP-219), secrets handling, dependency and license policy, and review
notes. No secret value ever lands in this repository: `.env.example` holds placeholder names only,
and anything that needs a real credential is a "Needs Justin" item.
