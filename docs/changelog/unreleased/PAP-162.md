### PAP-162 - Views parity checklist (Airtable, Notion, ClickUp, Baserow, NocoDB)

- Added `docs/research/views-parity-checklist.md`: 396 rows across 37 categories, every row citing a
  vendor document or vendor source file with its 2026-09-19 access date, tagged P0/P1/P2 and mapped to
  a `tables` PAP issue or `gap`. Includes the declined-for-v0.1 list and 15 gaps with acceptance criteria.
- Added `packages/views/src/parity/checklist.json` (source of truth), `linear-ids.json` (983-identifier
  allowlist) and `parity-report.mjs`: validates every row, prints coverage per product and per category,
  and regenerates `docs/research/views-parity-checklist.csv` and `views-parity-formula-functions.csv`
  (129 functions for PAP-171). Exits non-zero on a bad status, an unknown identifier or a missing source.
- No ADR. An ADR is owed for the declined features (GAP-15 in the checklist).
- Review pass (PAP-162): registered `pnpm --filter views parity:report` in
  `docs/reference/surfaces.md` (org standard: every CLI ability recorded in the same pass).
