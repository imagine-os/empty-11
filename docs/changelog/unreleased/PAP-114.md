### Added — PAP-114: page spec schema (`page.spec.yaml` v1)

- `@paperos/spec`: `PageSpecSchema` (Zod 4) with the eleven sections, reference grammars
  (`RouteRef`, `ComponentRef`, `MessageRef`, `PermissionRef`, ...), the actions registry
  (`logic.actions` → `pageActions()`), `status: not-wired` placeholders, `x-*` passthrough and the six
  reserved v1.1 keys; `parseSpec` / `validatePageSpec` with path, line, col and did-you-mean hints,
  duplicate-key and `status: ready` gate rules; `migrate/` skeleton (`SPEC_VERSIONS`, `Codemod`,
  `migrateSpec`); generated JSON Schema and field reference with a drift test.
- Paths: `packages/spec/` (src, schema, fixtures, scripts), `specs/pages/customer-invoices.spec.yaml`,
  `specs/pages/staff-settings.spec.yaml`, `docs/platform/page-spec.md` (generated),
  `docs/platform/page-spec-versioning.md`, `docs/reference/surfaces.md`, `docs/evidence/PAP-114/`.
- ADR: [0015-page-spec-schema](../../adr/0015-page-spec-schema.md)
