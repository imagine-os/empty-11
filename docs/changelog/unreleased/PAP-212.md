### PAP-212 — UI primitives decided: Base UI, with React Aria for dates

* Adopted `@base-ui/react` **1.8.0** as the headless foundation for `packages/ui`, and
  `react-aria-components` **1.21.1** scoped to date and time. ADR: `docs/adr/0029-ui-primitives-library.md`.
* The package name in the plan (`@base-ui-components/react`) is stale: Base UI renamed at 1.0.0
  (2025-12-11). Imports use `@base-ui/react`.
* Survey of five candidates with measured bundle, type-soundness, publint and advisory facts:
  `docs/research/ui-kits-and-headless-libraries.md`; scorecards in `docs/libraries/scorecards/`;
  gaps and fallbacks in `docs/libraries/primitives-gaps.md`.
* Measurement spike (standalone, excluded from `pnpm check`): `spikes/PAP-212-ui-kits/`.
