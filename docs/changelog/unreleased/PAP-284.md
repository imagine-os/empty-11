### Added — PAP-284: live roster as 37 validated character YAML files

- `packages/agents/roster.yaml` (round-4 defaults: leads `claude-opus-5` / high, read-mostly subs
  `claude-sonnet-5` / medium, Sentinel reviewers `plan` with Write and Edit denied; 700 USD day split
  30/20/12/10/8/8/5/4/3) and `packages/agents/characters/<name>.yaml` for the nine leads and 28 subs,
  all passing `validate`; `plan-to-roster` (convert, `--merge`, `--check`) and `tree [--check]`
  scripts with the pure modules under `packages/agents/src/roster/` and tests (shares sum, labels,
  inheritance, converter snapshot, plan diff).
- Paths: `packages/agents/roster.yaml`, `packages/agents/characters/`, `packages/agents/src/roster/`,
  `packages/agents/scripts/{plan-to-roster,tree}.ts`, `packages/agents/fixtures/plan-to-roster.snapshot.yaml`,
  `docs/platform/roster.md`, `docs/evidence/PAP-284/`.
- ADR: none (uses [0020-character-schema](../../adr/0020-character-schema.md)).
