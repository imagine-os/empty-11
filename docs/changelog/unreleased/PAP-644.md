### Added — PAP-644: shared Playwright input fixtures

- `@paperos/input/testing`: the `input` Playwright fixture with chords (`pressChord`, `sequence`,
  `holdKey` with auto-repeat), CDP touch (`tap`, `longPress`, `swipe`, `pinch`, `pan`) and pen
  strokes with pressure, a `navigator.getGamepads()` mock driven by contract button names, the
  PAP-330 keyboard drag grammar and a pointer drag, plus `expectModality`, `expectAnnouncement` and
  the `touchTargets` 44 px audit. Pure Playwright, no app import; CDP helpers skip loudly off Chromium.
- Paths: `packages/input/src/testing/`, `packages/input/e2e/`, `packages/input/playwright.config.ts`,
  `docs/platform/input/testing.md`, `docs/reference/surfaces.md`, `docs/evidence/PAP-644/`.
- ADR: none (no contract shape changed; builds on ADR 0019).
