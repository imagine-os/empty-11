### Added — PAP-103: character schema

- Zod 4 `CharacterSchema` / `RosterSchema` with the access-scope registry, known-tools list, price-table
  model ids and a stub MCP catalog; `validateRoster()` (17 error codes, 5 warnings) and
  `resolveInheritance()`; generated, drift-tested JSON Schema; golden fixtures for the nine leads and
  28 subs, one invalid fixture per code; `validate`, `gen:schemas`, `gen:fixtures`, `convert:plan`
  package scripts.
- Paths: `packages/agents/src/schema/`, `packages/agents/schema/`, `packages/agents/fixtures/`,
  `packages/agents/scripts/`, `docs/platform/character-schema.md`, `docs/evidence/PAP-103/`.
- ADR: [0020-character-schema](../../adr/0020-character-schema.md)
