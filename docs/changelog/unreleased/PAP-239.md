### Added — PAP-239: gate artifact contract (Quality)

- One schema package for every gate artifact: `GateReport<K>` envelope (`version: 1`, `sha`, `pr`, timestamps, `status`, `findings`, `summary`, `artifacts`, `data`) with a strict schema per kind (`gate1`, `security`, `perf`, `coverage`, `migrations`, `conformance`, `review`, `review-cost`, `visual`, `videos`, `vision`, `e2e`, `edgecases`, `flakes-delta`, `mutation`, `calibration`, `certification`); `ArtifactRef`, path conventions and `artifactUrl()` (Pages form, Forgejo run-artifact fallback, size limit); the `GATE_STATUSES` registry (16 statuses, typo-safe); `validateArtifact()` / `parseArtifact()` / `readGateReport()` and the `validate` CLI (exit 0/1/2); generated JSON Schemas and fixtures for every kind.
- Paths: `packages/contracts/quality/src/{status,artifacts,validate}.ts`, `src/gates/`, `scripts/validate.ts`, `schemas/*.schema.json`, `fixtures/`, `test/`; `docs/quality/gates.md` (section "Artifacts and statuses", generated tables); `docs/reference/surfaces.md`; `docs/evidence/PAP-239/`.
- Additive on PAP-79 (`Finding`, `findingId`, ADR 0023 unchanged); PAP-78's `packages/gate/src/gate1.ts` becomes a re-export.
- Owed root edits (PAP-13 owner): `"contracts:build"`, `"contracts:validate"` and `"gen:schemas"` aliases in the root `package.json` (Gate 1's drift step waits on `gen:schemas`).
