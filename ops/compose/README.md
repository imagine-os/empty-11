# ops/compose (stub)

Docker Compose stacks for local and staging runs (Postgres, MinIO, Hocuspocus, the API, the
worker). Owner: app-shell / Forge; the smoke workflow is ADR 0025 (PAP-754).

Infra files stay here until `paperos-infra` exists (PAP-25, waiting on the Hetzner account).
